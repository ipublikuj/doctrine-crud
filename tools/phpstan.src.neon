includes:
    - phpstan.base.neon

parameters:
    level: max

    paths:
        - ../src/

    bootstrapFiles:
        - phpstan-bootstrap.php

    resultCachePath: %currentWorkingDirectory%/var/tools/PHPStan/resultCache.src.php
